# Server Port (auto-assigned on Render)
PORT=5000

# Secret for signing JWT authentication tokens
JWT_SECRET=super_secret_legal_metrology_jwt_key_2024

# MongoDB Atlas Connection String
# Replace <username>, <password>, and <cluster-url> with your MongoDB Atlas details
# Example: mongodb+srv://admin:mysecretpassword@cluster0.abcde.mongodb.net/legal_metrology?retryWrites=true&w=majority
MONGODB_URI=
